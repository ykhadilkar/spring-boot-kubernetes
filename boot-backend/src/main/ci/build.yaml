---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: spring-boot-kubernetes/boot-backend

inputs:
  - name: spring-boot-kubernetes

run:
  path: spring-boot-kubernetes/boot-backend/src/main/ci/build.sh
